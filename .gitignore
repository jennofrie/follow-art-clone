# Node / build artifacts (none expected — this repo is pre-built)
node_modules/
dist/
build/
out/
.next/
.nuxt/

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
*.swo

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Secrets — never commit these
.env
.env.*
!.env.example
*.pem
*.key

# Netlify local
.netlify/

# Recon workspace lives elsewhere — don't let it leak in
*-recon/
playwright/
audit/
compare/
firecrawl/
